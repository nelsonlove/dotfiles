jdex:
  backend: yaml
notes:
  enabled: true
  backend: apple-notes
  sync:
    enabled: true
    direction: bidirectional
  filter:
    exclude_zeros: true
    exclude_status:
    - archived
  folders:
    style: area
