# macOS
.DS_Store

# Emacs generated
*.elc
*.eln
eln-cache/
*~
\#*\#
.\#*
emacs/custom.el
emacs/elgrep-data.el

# Local overrides
zsh/*.local

# zsh — compinit's generated completion cache (host/version specific)
zsh/.zcompdump*

# zsh — shell history (runtime data, machine-specific; not config)
zsh/.zsh_history
.zsh_history

# Backlog drafts
backlog/drafts/*

# Karabiner
karabiner/automatic_backups

# Inactive (legacy, kept for reference)
inactive/post-nix.sh

# Claude
.claude/backups
.claude/debug
.claude/file-history
.claude/local
.claude/paste-cache
.claude/plans
.claude/projects
.claude/shell-snapshots
.claude/statsig
.claude/tasks
.claude/telemetry
.claude/todos
.claude/plugins
.claude/stats-cache.json
.claude/cache
.claude/.checksums
.claude/history.jsonl

# Alacritty
alacritty/alacritty.toml.*.bak

# GitHub CLI (auth tokens)
gh/hosts.yml

# Micro
micro/buffers/history
micro/backups

# JD index (data, not config)
jd/jd-index.yaml

# Remember
.remember

# Semgrep — metrics state only (auth via SEMGREP_APP_TOKEN env var)
semgrep/

# Doom — auto-generated by Emacs's customize-* (theme trust, etc.)
doom/custom.el
claude/skills/*/scripts/bin/
